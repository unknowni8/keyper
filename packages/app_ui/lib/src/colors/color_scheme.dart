
  // ─────────────── PRIMARY ───────────────
  // The main brand color used for key UI elements.
  // final Color primary;

  // Color used for text/icons placed on top of the primary color.
  // final Color onPrimary;

  // A toned variant of primary, used for large colored areas like cards or chips.
  // final Color? primaryContainer;

  // Foreground color for content placed on primaryContainer.
  // final Color? onPrimaryContainer;

  // Fixed-tone primary color (doesn't change with elevation).
  // final Color? primaryFixed;

  // Dimmed version of primaryFixed, for inactive or disabled states.
  // final Color? primaryFixedDim;

  // Foreground color for content on primaryFixed surfaces.
  // final Color? onPrimaryFixed;

  // Softer variant of onPrimaryFixed for less contrast (subtle text/icons).
  // final Color? onPrimaryFixedVariant;      

  // ─────────────── SECONDARY ───────────────
  // Secondary brand color for less prominent components.
  // final Color secondary;

  // Foreground color for content on secondary color.
  // final Color onSecondary;

  // Background color for secondary containers like cards or chips.
  // final Color? secondaryContainer;

  // Foreground color for content on secondaryContainer.
  // final Color? onSecondaryContainer;       

  // Fixed-tone secondary color for consistent accent highlights.
  // final Color? secondaryFixed;

  // Dimmed version of secondaryFixed for inactive UI elements.
  // final Color? secondaryFixedDim;

  // Foreground color for content on secondaryFixed.
  // final Color? onSecondaryFixed;

  // Softer, less contrast version of onSecondaryFixed.
  // final Color? onSecondaryFixedVariant;      

  // ─────────────── TERTIARY ───────────────
  // Complementary accent color used for distinct or decorative UI.
  // final Color? tertiary;         

  // Foreground color for text/icons on tertiary color.
  // final Color? onTertiary;

  // Background color for tertiary containers.
  // final Color? tertiaryContainer;

  // Foreground color for content on tertiaryContainer.
  // final Color? onTertiaryContainer;

  // Fixed-tone tertiary color (constant tonal value).
  // final Color? tertiaryFixed;

  // Dimmed version of tertiaryFixed for inactive UI states.
  // final Color? tertiaryFixedDim;

  // Foreground color for content on tertiaryFixed.
  // final Color? onTertiaryFixed;

  // Softer variant of onTertiaryFixed (for subtle icons/text).
  // final Color? onTertiaryFixedVariant;

  // ─────────────── ERROR ───────────────
  // Used to indicate error or critical states (like input validation).
  // final Color error;

  // Foreground color for content on error surfaces.
  // final Color onError; 

  // Less intense error background for warnings or alerts.
  // final Color? errorContainer;

  // Foreground color for content on errorContainer.
  // final Color? onErrorContainer;

  // ─────────────── SURFACE & BACKGROUND ───────────────
  // Default surface color for UI elements (cards, sheets, etc.).
  // final Color surface;

  // Foreground color for content on surface.
  // final Color onSurface;

  // Darker variant of surface for lower layers (under dialogs, etc.).
  // final Color? surfaceDim;

  // Brighter variant of surface for elevated elements.
  // final Color? surfaceBright;

  // Lowest tonal surface container (base background layer).
  // final Color? surfaceContainerLowest;     

  // Slightly raised container surface.
  // final Color? surfaceContainerLow;     

  // Default container surface (mid-elevation).
  // final Color? surfaceContainer;   

  // Higher elevation surface (above mid-level containers).
  // final Color? surfaceContainerHigh;    

  // Top-most elevation surface (modals, sheets, dialogs).
  // final Color? surfaceContainerHighest;

  // Foreground color for content on surfaceVariant (secondary text/icons).
  // final Color? onSurfaceVariant;

  // ─────────────── OUTLINE, SHADOW & SCRIM ───────────────
  // Strong border or outline color for elements like TextFields or dividers.
  // final Color? outline;

  // Softer variant of outline for subtle borders.
  // final Color? outlineVariant;

  // Shadow color used for elevated elements like cards or FABs.
  // final Color? shadow;

  // Semi-transparent overlay used behind modals, dialogs, or drawers.
  // final Color? scrim;

  // ─────────────── INVERSE COLORS ───────────────
  // Surface color for inverse elements (dark on light or vice versa).
  // final Color? inverseSurface;

  // Foreground color for content on inverseSurface.
  // final Color? onInverseSurface;

  // Primary color used on inverse surfaces (like SnackBar text/icons).
  // final Color? inversePrimary;

  // Tint overlay used to show elevation depth on surfaces.
  // final Color? surfaceTint;

  // ─────────────── SYSTEM / OTHER ───────────────
  // Defines whether the color scheme is light or dark mode.
  // final Brightness brightness;
